spring:
  data:
    mongodb:
      uri: mongodb://${MONGO_USER:smartgig}:${MONGO_PASS:smartgig123}@mongodb:27017/${MONGO_DB:smartgig_analytics}?authSource=admin
  kafka:
    bootstrap-servers: kafka:29092

eureka:
  client:
    service-url:
      defaultZone: http://eureka-server:8761/eureka

management:
  zipkin:
    tracing:
      endpoint: http://zipkin:9411/api/v2/spans
